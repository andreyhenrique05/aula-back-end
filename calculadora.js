function somar(){
      const num1 = 10;
      const num2 = 20;
      
      const Total = num1 + num2;

      console.log(`A soma ${num1} + ${num2} = ${total}`);
}

function somar(){
    const num1 = 10;
    const num2 = 20;

    const Total = num1 + num2;

      console.log(`A divisão ${num1} + ${num2} = ${totaldiv}`);
}

function somar(){
    const num1 = 10;
    const num2 = 20;

    const Total = num1 + num2;

      console.log(`A A multiplicação ${num1} + ${num2} = ${totalmul}`);
}

function somar(){
    const num1 = 10;
    const num2 = 20;

    const Total = num1 + num2;

      console.log(`A subtração ${num1} + ${num2} = ${totalsub}`);
}

function somar2 (num1, num2){
    const total= num1 + num2;

    console.log(`A soma ${num1} + ${num2} = ${total}`);
}

function somar3 (num1, num2){
    const num1 = 10;
    const num2 = 20;

    const total= num1 + num2;
    
    console.log(`A soma ${num1} + ${num2} = ${total}`);
}

//chamando as funções para executar-las 
 somar2();
 somar3();

 // agora que conhecemos as funções, vms criar uma calculadora  em javascript.
 // crie 4 funções, das operações de soma, multiplicação, divisão e subtração 

 // quais são ps operadores de soma, multiplicação, divisão e subtração 
// + (soma)
// - (subtração)
// * (multiplicação)
// /  (divisão)
